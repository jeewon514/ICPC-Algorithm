//친구 - 10864(Bronze2)
#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N,M;
    cin>>N>>M;

    int A,B;
    for (int i = 0; i < M; i++) {
        cin>>A>>B;
        //cout<<A<<B<<'\n';
    }

    int count = 0;
    for (int i = 1; i <= N; i++) {
        if (i == ) {
            cout << count++ << endl;
        }
    }

    return 0;
}
